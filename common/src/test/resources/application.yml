server:
  # Random port to avoid conflicts
  port: 0

spring:
  application:
    name: web-api
  profiles:
    active:
      - ci
  redis:
    host: localhost
    port: 6379
    ssl: false
    password:

application:
  cache:
    ttl-second: 1
